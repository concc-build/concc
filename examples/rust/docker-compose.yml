version: '3'

services:
  client:
    image: rust-buildenv
    init: true
    volumes:
      - ./workspace:/workspace
    secrets:
      - concc
    environment:
      RUSTC_WRAPPER: /usr/local/bin/concc-dispatch

  worker:
    image: rust-buildenv
    init: true
    command: >-
      concc-worker
    cap_add:
      - SYS_ADMIN
    devices:
      - /dev/fuse

  project:
    image: atmoz/sftp  # Use corilus/sftp for debugging
    init: true
    ports:
      - '2222:22/tcp'
    volumes:
      - ./workspace:/home/concc/workspace
      - ./secrets.txt:/etc/sftp/users.conf:ro
    secrets:
      - concc

secrets:
  concc:
    file: ./secrets.txt
