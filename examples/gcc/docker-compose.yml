version: '3'

services:
  client:
    image: gcc-buildenv
    init: true
    volumes:
      - ./project:/workspace/project

  worker:
    image: gcc-buildenv
    init: true
    command: >-
      concc-worker
    cap_add:
      - SYS_ADMIN
    devices:
      - /dev/fuse:/dev/fuse
